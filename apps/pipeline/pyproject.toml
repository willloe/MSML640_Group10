[project]
name = "slides-pipeline"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = ["slides-classifier", "slides-diffusion"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
